name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
    build-and-check:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                service:
                    - services/auth_service
                    - services/event_service
                    - services/booking_service
                    - gateway
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Node.js
              uses: actions/setup-node@v4
              with: 
                node-version: '22'
                cache: 'npm'
                cache-dependency-path: ${{ matrix.service }}/package-lock.json
            
            - name: Install dependencies
              working-directory: ${{ matrix.service }}
              run: npm ci

            - name: Generate Prisma client (if applicable)
              working-directory: ${{ matrix.service }}
              run: |
                if [ -f "prisma/schema.prisma" ]; then
                  npx prisma generate
                else
                    echo "No Prisma schema found for ${{ matrix.service }}, skipping Prisma client generation."
                fi
            
            - name: Type check
              working-directory: ${{ matrix.service }}
              run: npx tsc --noEmit

            - name: Build Docker image (verification only, not pushed yet)
              run: docker build -t temp-${{ matrix.service }}-check ${{ matrix.service }}
                    