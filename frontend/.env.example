# Gateway API (microservices entry point — used by Next.js rewrites)
GATEWAY_URL=http://localhost:5000

# Booking service WebSocket (direct — not proxied through gateway)
NEXT_PUBLIC_BOOKING_WS_URL=http://localhost:5002
